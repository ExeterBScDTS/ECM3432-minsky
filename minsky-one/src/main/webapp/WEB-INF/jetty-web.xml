<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE Configure PUBLIC "-//Jetty//Configure//EN" "http://www.eclipse.org/jetty/configure.dtd">

<Configure>
    <Set name="contextPath">/minskyOne-0.2</Set>

    <Set name="war"><SystemProperty name="jetty.home" default="."/>/</Set>


    <Get name="securityHandler">
    <!--
    <Set name="loginService">
      <New class="org.eclipse.jetty.security.HashLoginService">
            <Set name="name">Test Realm</Set>
            <Set name="config"><SystemProperty name="jetty.home" default="."/>/src/etc/realm.properties</Set>
      </New>
    </Set>
    -->
    <!--
     <Set name="loginService">
      <New class="org.eclipse.jetty.security.JDBCLoginService">
            <Set name="name">Test Realm</Set>
            <Set name="config"><SystemProperty name="jetty.home" default="."/>/src/etc/jdbcRealm.properties</Set>
      </New>
    </Set>
    -->
    </Get>
</Configure>
